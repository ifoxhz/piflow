---
title: "piFlow 用户使用手册"
subtitle: "Windows 便携版 · v0.3"
author: "piFlow"
---

# piFlow 用户使用手册

**版本**：v0.3  
**适用平台**：Windows 10 / 11（便携包）  
**更新日期**：2026-08

---

## 1. 产品简介

**piFlow** 是一款本地优先的检索增强生成（RAG）桌面应用。你可以将本机文档导入知识库，再用自然语言提问；系统会基于你的文档检索相关内容并生成回答，同时给出可点击的**来源引用**。

主对话为基于 Pi 的智能 Agent，可通过可开关的 **Skill** 查询知识库、Postgres（只读）或操作本机工作区文件。

### 1.1 核心特点

| 特点 | 说明 |
|------|------|
| 答案可追溯 | 回答附带引用与原文摘录，可定位到具体文档片段 |
| 隐私优先 | 文档、向量索引与对话数据保存在本机，默认不上传云端 |
| 目录导入 | 选择文件夹即可递归导入；后台处理，界面显示进度与预计时间 |
| 本地嵌入 | 使用本机 BGE-M3 模型做向量化（首次导入会占用一定时间与 CPU） |
| 可选远端生成 | 可在设置中配置 Ollama 或 DeepSeek（互斥） |
| piFlow Agent | 主对话入口；Knowledge / Postgres 只读 / Local FS 等 Skill 可分开启用 |

### 1.2 本手册覆盖范围

- Windows **便携包**的安装、启动与日常使用  
- 知识库导入、**piFlow** 主对话、设置、常见问题  

> **说明**：开发者编译、WSL 调试等内容请参阅 `docs/development-windows.md`。piFlow 技术设计见 `docs/piflow.md`。

---

## 2. 系统要求

| 项目 | 建议配置 |
|------|----------|
| 操作系统 | Windows 10 / 11（64 位） |
| 内存 | 建议 **16 GB** 及以上 |
| 磁盘 | 至少 **10 GB** 可用空间（含嵌入模型与首次解压） |
| 运行时 | 需已安装 **WebView2**（Win10/11 通常已预装） |
| 网络（可选） | 若使用远端 Ollama，需能访问对应主机与端口（默认 `11434`） |
| piFlow Local FS（可选） | 使用 `bash` 工具时需安装 **Git for Windows**（提供 `bash.exe`） |
| piFlow Postgres（可选） | 可访问的 PostgreSQL 实例 |

便携包已内置：

- 桌面程序（`piFlow.exe`）  
- Node 运行时与 RAG 后端（首次启动自动解压）  
- 嵌入模型 **BGE-M3**  

首版便携包**默认不包含**：本地大模型 GGUF、OCR 扫描版 PDF 增强（扫描件效果可能较差）。

---

## 3. 安装与启动

### 3.1 获取与解压

1. 获得压缩包，例如：`piFlow-0.1.0-portable.zip`  
2. 解压到较短路径，例如：`D:\apps\piFlow`  
   - 避免过深路径或仅含中文/特殊符号的路径，以减少首次解压失败概率  
3. 进入解压目录（文件夹名一般为 `piFlow`），确认至少包含：

| 文件 / 目录 | 作用 |
|-------------|------|
| `piFlow.exe` | 主程序 |
| `runtime\` | 内置 Node 运行时 |
| `models\` | 嵌入模型等 |
| `rag-server.zip` | RAG 后端包（首次启动解压） |
| `README.txt` | 简要说明 |

### 3.2 首次启动

1. 双击 **`piFlow.exe`**  
2. 首次启动会自动将后端解压到：  
   `%APPDATA%\piFlow\sidecar\`  
3. 等待窗口打开；后端默认监听本机端口 **`3847`**  
4. 若杀毒软件拦截，请将安装目录与上述 AppData 目录加入白名单后重试  

> **说明**：知识库与对话历史保存在 `%APPDATA%\piFlow\`（SQLite，含向量），**不在**便携 zip 内。重新解压安装包不会清空旧数据；若要全新环境，请先退出程序并删除该目录。便携包仅附带空库种子（`seed/piflow.db`），仅在数据目录尚无数据库时用于初始化。

### 3.3 日常启动

之后直接运行 `piFlow.exe` 即可；一般无需再手动启动其他服务。

### 3.4 数据存放位置

| 内容 | 大致位置 |
|------|----------|
| 用户数据 / 数据库 / 日志 | `%APPDATA%\piFlow\` |
| 后端解压目录 | `%APPDATA%\piFlow\sidecar\` |
| 开发态数据目录 | 仓库内 `.data\`（含 `piflow.db`、Ollama / Postgres / piFlow 配置） |

卸载或清理时：退出程序后删除便携文件夹；如需彻底清除数据，再删除上述 AppData 目录。

---

## 4. 界面概览

主窗口大致分为：

| 区域 | 功能 |
|------|------|
| **piFlow（主对话）** | Pi Agent：知识库检索（kb tools）+ Postgres 只读 + 可选本地文件；会话与 Sources 引用 |
| **Knowledge Base（知识库）** | 导入文件夹、查看已索引文档、导入进度与活动日志（不承担主问答） |
| **Settings（设置）** | 模型（Ollama/DeepSeek）、Postgres、Local FS / Knowledge Skill、服务状态等 |

侧边栏 **New Chat** 新建 **piFlow** 会话；会话列表为 piFlow 历史。导入文档后，piFlow 的 Knowledge skill 状态会更新为「已就绪」。

---

## 5. 配置生成服务（Settings → Ollama）

piFlow 在本机完成向量检索；对话生成推荐连接 **Ollama** 或 Settings 中的 **DeepSeek**（互斥）。导入仍使用本机 BGE-M3。

### 5.1 配置步骤

1. 打开 **Settings**  
2. 填写 **Ollama URL**，例如：`http://10.0.0.7:11434`  
3. 填写 **模型名**，例如：`qwen3.5:4b`（需与 Ollama 中已拉取的模型一致）  
4. 可选填写 **中文模型**  
5. 点击 **确定 / 保存**（配置立即生效）  
6. 查看连通状态；若显示不可达，请检查网络与防火墙后再试  

### 5.2 未配置生成服务时

- **知识库导入**：不依赖 Ollama / DeepSeek，仍可导入与建索引。  
- **piFlow 对话**：需在 Settings 中配置 **Ollama** 或 **DeepSeek**（二者互斥）之一且可达，否则无法发起 Agent 对话。

### 5.3 Ollama 侧准备（管理员 / 运维）

在 Ollama 所在机器上：

```text
ollama pull <你的模型名>
```

并确认 Windows 客户端能访问：`http://<主机>:11434/api/tags`。

---

## 6. 导入知识库

### 6.1 支持的文件类型

| 类型 | 扩展名 |
|------|--------|
| PDF | `.pdf`（文本层 PDF 效果最佳） |
| Markdown | `.md` / `.markdown` |
| 纯文本 | `.txt` |
| HTML | `.html` / `.htm` |

> 扫描版 / 纯图片 PDF：首版便携包默认关闭 OCR，识别效果可能不佳。请优先使用可选中文字的 PDF。

### 6.2 导入步骤

1. 打开 **Knowledge Base**  
2. 点击 **Import folder**（导入文件夹）  
3. 在系统对话框中选择包含文档的目录（会递归处理子目录）  
4. 确认后开始后台导入  

### 6.3 进度说明

导入过程中你可能看到：

| 信息 | 含义 |
|------|------|
| 当前文件名 | 正在处理的文件 |
| 预计时间（ETA） | 根据页数 / 块数估算的剩余时间 |
| `N / M pages` | PDF 按页窗口进度（默认每窗约 20 页：解析→切块→向量化） |
| `N / M chunks` | 向量化进度（文本块）；指纹未变的页会显示「复用」而不重算向量 |

说明：

- **解析**通常较快；大部分时间花在本机 **BGE-M3 嵌入**（CPU 上约每块数秒量级，大文档会明显更久）。  
- 进度会持续更新；请耐心等待，勿重复多次点击导入同一目录。  
- 未变更的文件可能被自动**跳过**。  
- 首版同一时间通常只运行 **一个** 导入任务。

### 6.4 导入完成后

- 文档列表中会出现已索引文件  
- 底部活动日志会显示成功 / 跳过 / 失败汇总  
- 失败项请检查文件是否损坏、是否受权限限制，或是否为不支持的格式  

### 6.5 体积与时间参考（经验值）

| 文档规模 | 粗略预期 |
|----------|----------|
| 数页～十几页 PDF | 约 1～数分钟 |
| 百页级文字 PDF（约百余 chunks） | 约数分钟～十余分钟（视 CPU 而定） |
| 数百页大书 | 可能数十分钟，请关注 chunk 进度与 ETA |

---

## 7. 对话提问（piFlow · 知识库）

主问答入口是侧栏 **piFlow**（不再有独立的旧版 Chat 页）。导入文档并启用 Knowledge skill 后，即可用自然语言问本地知识库；Agent 通过 `kb_*` 工具检索，并在回答下方展示 **Sources** 引用。

### 7.1 基本用法

1. 确保已至少成功导入部分文档（Knowledge skill 显示就绪）  
2. 打开侧栏 **piFlow**，或点击 **New Chat** 新建会话后提问  
3. 在输入框输入问题，例如：  
   - 「这份白皮书的主要结论是什么？」  
   - 「文档里提到了哪些人名？」  
   - 「某某功能在哪一节说明？」  
4. 发送后等待回答；过程中可能出现 `tool → kb_search` 等提示  

首次提问或长时间空闲后，嵌入模型可能有短暂**冷启动**，首条可能稍慢。

### 7.2 引用与溯源

回答下方通常会显示 **Sources（引用）**：

- 来源文档名称  
- 相关段落摘录（及页码等元数据，若有）  
- 可据此核对原文，降低「幻觉」风险  

若引用为空或很少：可能是问题与知识库无关，或文档尚未导入完成 / 文本层过少。

### 7.3 多轮对话与会话管理

- 同一会话中可继续追问；重要新主题建议点击 **New Chat**  
- **切换 / 重命名 / 删除**：在侧边栏会话列表操作  

---

## 8. piFlow（工作流 Agent）

**piFlow** 是主对话界面：按已启用的 **Skill** 调用工具。启用 Knowledge 时可检索你导入的文档（`kb_*`）；还可按需启用 Postgres 只读、Local FS 等。

### 8.1 打开 piFlow

1. 点击侧栏导航 **piFlow**（或 **New Chat**）  
2. 顶部可见当前 Skill 是否启用 / 就绪；会话列表在侧栏  
3. 右侧输入自然语言问题并发送  

配置入口在 **Settings**（模型、Knowledge / Postgres / Local FS 等）。

### 8.2 Skill：Postgres 只读

用于用自然语言查询 PostgreSQL（**只读**，禁止 DELETE / DROP 等）。

**配置：**

1. 打开 **Settings → Postgres（piFlow skill）**  
2. 勾选 **启用 Postgres 只读 skill**  
3. 填写 Host / Port / Database / User / Password（可选 SSL）  
4. 可先点 **测试连接**，再点 **保存连接**（保存时会预热 schema 缓存）  

**示例问题：**

- 「最后一个患者是谁，列出他的资料」  
- 「患者 ID 145 有多少个 study？」  

**说明：**

- 连接信息保存在本机（开发态为 `.data/postgres-config.json`）  
- Agent 只会执行只读 SQL；破坏性语句会被拦截  
- 需要已配置且可达的生成服务（Ollama 或 DeepSeek）  

### 8.3 Skill：Local FS（本地文件）

用于在指定**工作区目录**内读文件、用 bash 辅助、按需写文件。与 Postgres Skill **分开开关**。

**配置：**

1. 打开 **Settings → Local FS（piFlow skill）**  
2. 勾选 **启用本地文件 skill**  
3. 填写工作区**绝对路径**（必须是已存在的文件夹），例如：`D:\dev\my-project`  
4. 可选：是否 **允许写入**（关闭则仅 `read` / `bash`）  
5. 点击 **保存 Local FS / skill 开关**  

**Windows 注意：**

- 使用 `bash` 前请安装 [Git for Windows](https://git-scm.com/download/win)，否则 bash 工具会失败  
- 请勿把工作区设成整个系统盘；只指向你允许 Agent 接触的项目目录  
- 即使允许写入，**删除文件 / 破坏性清理**仍被 no-delete 策略禁止  

**示例问题：**

- 「列出工作区根目录有哪些文件」  
- 「打开 README.md 并总结」  
- 「在 docs 下新建一个 draft.md，写上今日纪要」（需开启允许写入）  

### 8.4 piFlow 会话

- 点击侧栏 **New Chat** 开始新会话（也可在空会话直接提问，系统会自动创建会话）  
- 会话保存在本机服务端数据库（`%APPDATA%\piFlow\` / 开发态 `.data\`）  
- 对话中可能出现 `tool → kb_search` / `tool → pg_query` / `tool → read` 等提示，表示 Agent 正在调用工具  
- 请等待完整回答；若长时间无结果，检查生成服务（Ollama/DeepSeek）、Skill 配置与（Local FS）Git Bash  

**备注：同一问题答案为何会略有差异？**

对同一问题多次提问时，回答在措辞、要点取舍或侧重点上可能略有不同。这通常不是知识库内容变了，而是 **Agent 编排（agentic RAG）** 与 **生成式解码** 的固有特性。

主要原因：

1. **生成采样随机性（stochastic decoding）**  
   LLM 在非零温度下做概率采样；即使证据相近，**摘要综合（abstractive synthesis）** 的表述与详略仍可能漂移。

2. **非确定性工具规划（non-deterministic tool planning）**  
   每轮由模型自行决定调用哪些 `kb_*`、检索 query 与次数；**检索轨迹**不同 → 进入上下文的 **证据集** 不同。

3. **稠密检索的查询敏感性（query-sensitive dense retrieval）**  
   向量检索对 query 表述敏感；同义改写可能改变召回集与排序。

4. **多跳检索噪声累积（multi-hop retrieval noise）**  
   多次 `kb_search` 引入低相关命中后，模型对「哪些事实写入结论」的取舍会漂移。

缓解思路：限制每轮检索次数并规范「宽检索 → 有限补检索 → 作答」；回合内对近似 query 去重、对 citation 截断；事实问答使用更低 `temperature`；需要强一致时可对规范化问题做短时答案缓存，或采用「先检索、再只综合」的 grounded 模式。

重要结论请对照回答下方的 **Sources / 引用原文**。

### 8.5 安全提示

| 项 | 说明 |
|----|------|
| 无删除 | `no-delete-data` 始终生效：不删库、不删文件 |
| Postgres | 仅只读 SQL |
| Local FS | 限制在你配置的工作区内；写操作可单独关闭 |
| 生成服务 | 提示词与工具结果会发往你配置的 Ollama 或 DeepSeek |

---

## 9. 使用建议与最佳实践

1. **先导入、再提问（RAG）**：导入完成后再做正式问答。  
2. **目录整理**：将同一主题文档放在同一文件夹，减少无关噪声。  
3. **优先文本 PDF**：可复制文字的 PDF 远优于纯扫描件。  
4. **问题尽量具体**：带上文档名、术语、时间范围，检索更准。  
5. **核对引用**：重要结论请点开 Sources / 引用对照原文。  
6. **生成服务放近一点**：局域网 Ollama 或稳定的 DeepSeek 连接通常体验更好。  
7. **piFlow 分 Skill 使用**：只查知识库时保持 Local FS 关闭，减少模型误调 bash。  
8. **Local FS 工作区收窄**：只授权必要项目目录，并按需关闭写入。  

---

## 10. 隐私与安全

- 文档内容与向量默认保存在**本机** SQLite / 本地目录。  
- 配置远端 Ollama / DeepSeek 时，**检索到的相关片段、piFlow 提示词与工具结果**会发往对应服务；请仅连接你信任的后端。  
- Postgres 密码保存在本机配置文件中，请妥善保护用户数据目录。  
- 请勿将便携目录与 AppData 中的数据库随意拷贝到不可信环境。  

---

## 11. 常见问题（FAQ）

| 现象 | 可能原因与处理 |
|------|----------------|
| 双击无反应 / 闪退 | 检查 WebView2；杀毒拦截；换短路径解压；查看 AppData 下日志 |
| 窗口打开但导入/聊天失败 | 后端未就绪或端口 `3847` 被占用；退出后重开，或结束占用该端口的进程 |
| Ollama「暂不可达」 | 检查 URL、模型名、防火墙与远端服务；配置仍可保存，修好后再聊 |
| 导入很久进度不动 | 大文件嵌入本身较慢；关注 `N / M chunks`；若进程崩溃再重试 |
| 扫描 PDF 几乎无内容 | 首版便携默认弱 OCR；请换文本层 PDF 或后续开启 OCR 版本 |
| 回答很空或只有摘要 | 未配置 / 未连通生成服务（Ollama 或 DeepSeek）；配好后再问 |
| 换电脑后知识库没了 | 数据在原机器 `%APPDATA%\piFlow\`；需迁移该目录或重新导入 |
| 端口被占用 | 其他 piFlow / 开发态服务占用了 `3847`；只保留一个实例 |
| piFlow 半截无响应 | 确认生成服务正常；Postgres/Local FS 是否就绪；Local FS 是否缺 Git Bash；可新开对话重试 |
| piFlow bash 报错 | Windows 未装 Git Bash，或工作区路径无效 |
| Postgres 工具提示未配置 | Settings 中保存连接，并确认「启用 Postgres 只读 skill」已勾选 |
| Settings 底部看不见 | 在设置页内向下滚动；内容较长时主区域可滚动 |

---

## 12. 附录

### 12.1 默认端口与路径

| 项 | 值 |
|----|-----|
| RAG / piFlow 服务 | `http://127.0.0.1:3847` |
| 健康检查 | `http://127.0.0.1:3847/health` |
| 用户数据（便携） | `%APPDATA%\piFlow\` |
| 开发态数据 | 仓库 `.data\` |

### 12.2 版本信息

- 产品版本：与便携包 `README.txt` / `BUNDLE_INFO.json` 中标注一致  
- 手册版本：**v0.3**（便携包包名统一为 `piFlow-*-portable.zip`；主对话为 piFlow + Knowledge skill）  

### 12.3 获取帮助

- 产品说明：仓库根目录 `README.md`  
- Windows 开发与打包：`docs/development-windows.md`  
- 架构说明：`docs/architecture.md`  
- piFlow 设计：`docs/piflow.md`  

---

*感谢使用 piFlow。本手册面向最终用户；功能以当前发行版为准。*
